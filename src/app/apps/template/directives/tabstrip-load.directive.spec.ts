import { TabstripLoadDirective } from './tabstrip-load.directive';

describe('TabstripLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new TabstripLoadDirective();
    expect(directive).toBeTruthy();
  });
});
